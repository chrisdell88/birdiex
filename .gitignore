# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — never commit
.env
.env.*
!.env.example

# Pipeline output (raw API dumps)
data/raw/
# Transient auto-roll notify marker (runtime only — read by the workflow after push)
data/.pending-notify.json

# Claude Code local config (worktrees, launch.json, etc.)
.claude/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.gstack/
